### Creatign Models ###
A new WCModel called 'name' is saved.
A new HDF5 file called 'name'.hdf5 is created with the following structure:
	/
		processes : str
		options : str
		parameters : str
		metadata : str
		states/  
'wcdb.hdf5' is opened. A new external link to 'name'.hdf5 is added to "/models/"

### Adding burps to Models, where burps is in			### 
###          [processes, options, parameters, metadata] ###
A new burp called 'p1' is added to the model.
'name.hdf5' is opened, and a new str is added to the "/burps" dataset


### Adding 

